edit 

<h1>Edit Team</h1>

<% if @team.errors.any? %>
  <div class="alert alert-danger">
    <h2>The following errors prevented the team from being saved:</h2>
    <ul>
      <% @team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li> 
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @team, local: true, class: "edit-team-form") do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control", placeholder: "Enter team name" %>
  </div>

  <div class="form-group">
    <%= f.label :country %>
    <%= f.text_field :country, class: "form-control", placeholder: "Enter country" %>
  </div>

  <div class="form-group">
    <%= f.label :founded %>
    <%= f.number_field :founded, class: "form-control", placeholder: "Enter founded year" %>
  </div>

  <div class="form-group">
    <%= f.label :player_count %>
    <%= f.number_field :player_count, class: "form-control", placeholder: "Enter player count" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: "form-control", placeholder: "Enter description" %>
  </div>

  <div class="form-actions">
    <%= f.submit "Update Team", class: "btn btn-primary" %>
    <%= link_to 'Cancel', teams_path, class: 'btn btn-secondary' %>
  </div>
<% end %>


index


<table class="table table-bordered">
  <thead class="thead-dark">
    <tr>
      <th>Name</th>
      <th>Country</th>
      <th>Founded</th>
      <th>Player Count</th>
      <th>Description</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td><%= team.country %></td>
        <td><%= team.founded %></td>
        <td><%= team.player_count %></td>
        <td><%= team.description %></td>
        <td>
          <%= link_to 'Show', team_path(team), class: 'btn btn-info btn-sm mr-1' %>
          <%= link_to 'Delete', team_path(team), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm mr-1' %>
          <%= link_to 'Edit', edit_team_path(team), class: 'btn btn-primary btn-sm' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br><br><br>

<%= link_to "New Team", new_team_path, class: 'btn btn-success' %>

new

<h1>Create a New Team</h1>

<% if @team.errors.any? %>
  <div class="alert alert-danger">
    <h2>The following errors prevented the team from being saved:</h2>
    <ul>
      <% @team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li> 
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(scope: :team, url: teams_path, local: true, class: "new-team-form") do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control", placeholder: "Enter team name" %>
  </div>

  <div class="form-group">
    <%= f.label :country %>
    <%= f.text_field :country, class: "form-control", placeholder: "Enter country" %>
  </div>

  <div class="form-group">
    <%= f.label :founded %>
    <%= f.text_field :founded, class: "form-control", placeholder: "Enter founded year" %>
  </div>

  <div class="form-group">
    <%= f.label :player_count %>
    <%= f.text_field :player_count, class: "form-control", placeholder: "Enter player count" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: "form-control", placeholder: "Enter description" %>
  </div>
  
  <div class="form-actions">
    <%= f.submit "Create Team", class: "btn btn-primary" %>
    <%= link_to "Cancel", teams_path, class: "btn btn-secondary" %>
  </div>
<% end %>

show

<h1>Team Details</h1>

<div class="card">
  <div class="card-body">
    <p class="card-text"><strong>Name:</strong> <%= @team.name %></p>
    <p class="card-text"><strong>Country:</strong> <%= @team.country %></p>
    <p class="card-text"><strong>Founded:</strong> <%= @team.founded %></p>
    <p class="card-text"><strong>PlayerCount:</strong> <%= @team.player_count %></p>
    <p class="card-text"><strong>Description:</strong> <%= @team.description %></p>
    
    <%= link_to "Edit", edit_team_path(@team), class: "btn btn-primary mr-2" %>
    <%= link_to "Back", teams_path, class: "btn btn-secondary" %>
  </div>
</div>
